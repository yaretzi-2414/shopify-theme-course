<div>
    <h1>
        {%- assign image = 'hero-image.svg' | asset_url -%}
        <img loading="lazy" width="{{ image.width }}" height="{{ image.height }}" src="{{ image | image_url }}"  />
        <span>vintage & thrift</span>
    </h1>
</div>


{% schema %}
{
    "name": "Hero",
    "presets": 
    [
        {
        "name":"Hero"
        }
    ]
}
{% endschema %}